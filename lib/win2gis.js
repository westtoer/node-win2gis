/*jslint node: true*/

"use strict";
var fs = require('fs'),
    path = require('path'),
    util = require('util'),
    http = require('http'),
    moment = require('moment');

module.exports.convert = function (inf, outf) {
    console.log("todo implement the conversion from %s to %s", inf, outf);
};
